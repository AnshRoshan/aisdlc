---
description: Handle any change of intent after the spec is signed - new requirement, changed behaviour, removed scope, a bug that reveals the spec was wrong
---
Load and follow the `aisdlc-delta` skill in this repository (.claude/skills/aisdlc-delta/SKILL.md). Start by running `npx aisdlc next $ARGUMENTS` and obey its output.
