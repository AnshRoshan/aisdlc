---
description: Compact the current session into a handoff document so a fresh session, a different agent, or a human can continue without loss
---
Load and follow the `aisdlc-handoff` skill in this repository (.claude/skills/aisdlc-handoff/SKILL.md). Start by running `npx aisdlc next $ARGUMENTS` and obey its output.
